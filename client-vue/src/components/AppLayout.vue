<!-- 逐行注释标记：已按业务语义处理，仅增加中文注释，不改变页面逻辑 -->
<!--
  <!-- 行注释：声明页面结构或组件属性，组成用户看到的前端界面。 -->
  答辩注释:
  <!-- 行注释：声明页面结构或组件属性，组成用户看到的前端界面。 -->
  - 文件： AppLayout
  <!-- 行注释：声明页面结构或组件属性，组成用户看到的前端界面。 -->
  - 层级：前端组件层
  <!-- 行注释：声明页面结构或组件属性，组成用户看到的前端界面。 -->
  - 职责：提供可复用的布局外壳、侧边导航和顶部用户操作区域。
  <!-- 行注释：声明页面结构或组件属性，组成用户看到的前端界面。 -->
  - 此注释仅用于说明，页面行为保持不变。
-->
<!-- 行注释：声明 Vue 模板区域，下面写页面显示出来的结构。 -->
<template>
  <!-- 行注释：声明页面结构或组件属性，组成用户看到的前端界面。 -->
  <a-layout style="min-height: 100vh">
    <!-- 行注释：声明页面结构或组件属性，组成用户看到的前端界面。 -->
    <a-layout-sider v-model:collapsed="collapsed" collapsible>
      <!-- 行注释：声明页面结构或组件属性，组成用户看到的前端界面。 -->
      <div class="logo-wrap">
        <!-- 行注释：把响应式变量渲染到页面上，数据变化时页面会自动更新。 -->
        <h2>{{ collapsed ? '农场' : '农场能源管理' }}</h2>
      <!-- 行注释：结束当前标签，表示该区域的页面结构收尾。 -->
      </div>

      <!-- 行注释：声明页面结构或组件属性，组成用户看到的前端界面。 -->
      <a-menu
        theme="dark"
        mode="inline"
        :selected-keys="[route.path]"
        :items="menuItems"
        @click="onMenuClick"
      />
    <!-- 行注释：结束当前标签，表示该区域的页面结构收尾。 -->
    </a-layout-sider>

    <!-- 行注释：声明页面结构或组件属性，组成用户看到的前端界面。 -->
    <a-layout>
      <!-- 行注释：声明页面结构或组件属性，组成用户看到的前端界面。 -->
      <a-layout-header class="header">
        <!-- 行注释：声明页面结构或组件属性，组成用户看到的前端界面。 -->
        <h3>农场能源管理系统</h3>
        <!-- 行注释：声明页面结构或组件属性，组成用户看到的前端界面。 -->
        <div class="header-right">
          <!-- 行注释：把响应式变量渲染到页面上，数据变化时页面会自动更新。 -->
          <a-tag color="blue">{{ userRoleLabel }}</a-tag>
          <!-- 行注释：把响应式变量渲染到页面上，数据变化时页面会自动更新。 -->
          <span class="username">{{ userDisplayName }}</span>
          <!-- 行注释：渲染操作按钮，触发新增、保存、删除或查询动作。 -->
          <a-button size="small" @click="logout">退出登录</a-button>
        <!-- 行注释：结束当前标签，表示该区域的页面结构收尾。 -->
        </div>
      <!-- 行注释：结束当前标签，表示该区域的页面结构收尾。 -->
      </a-layout-header>

      <!-- 行注释：声明页面结构或组件属性，组成用户看到的前端界面。 -->
      <a-layout-content style="margin: 24px 16px 0">
        <!-- 行注释：声明页面结构或组件属性，组成用户看到的前端界面。 -->
        <div class="content-wrap">
          <!-- 行注释：声明页面结构或组件属性，组成用户看到的前端界面。 -->
          <slot />
        <!-- 行注释：结束当前标签，表示该区域的页面结构收尾。 -->
        </div>
      <!-- 行注释：结束当前标签，表示该区域的页面结构收尾。 -->
      </a-layout-content>
    <!-- 行注释：结束当前标签，表示该区域的页面结构收尾。 -->
    </a-layout>
  <!-- 行注释：结束当前标签，表示该区域的页面结构收尾。 -->
  </a-layout>
<!-- 行注释：结束模板区域，页面结构到这里完成。 -->
</template>

<!-- 行注释：进入脚本区域，下面编写数据请求和页面交互逻辑。 -->
<script setup>
// 行注释：引入当前页面或组件需要的依赖。
import { computed, h, ref } from 'vue';
// 行注释：引入当前页面或组件需要的依赖。
import { useRoute, useRouter } from 'vue-router';
// 行注释：引入当前页面或组件需要的依赖。
import {
  // 行注释：承接上方业务语句，继续完成当前流程的数据组装或调用。
  DashboardOutlined,
  // 行注释：承接上方业务语句，继续完成当前流程的数据组装或调用。
  ThunderboltOutlined,
  // 行注释：承接上方业务语句，继续完成当前流程的数据组装或调用。
  BulbOutlined,
  // 行注释：承接上方业务语句，继续完成当前流程的数据组装或调用。
  CloudOutlined,
  // 行注释：承接上方业务语句，继续完成当前流程的数据组装或调用。
  LineChartOutlined,
  // 行注释：承接上方业务语句，继续完成当前流程的数据组装或调用。
  FileTextOutlined,
  // 行注释：承接上方业务语句，继续完成当前流程的数据组装或调用。
  TeamOutlined,
  // 行注释：承接上方业务语句，继续完成当前流程的数据组装或调用。
  AuditOutlined
// 行注释：承接上方业务语句，继续完成当前流程的数据组装或调用。
} from '@ant-design/icons-vue';
// 行注释：引入当前页面或组件需要的依赖。
import api from '../api/http';
// 行注释：引入登录状态工具，用于读取 token 或清空会话。
import { authState, clearSession, currentRole } from '../auth/session';

// 行注释：声明变量或响应式状态，支撑页面数据和交互流程。
const route = useRoute();
// 行注释：声明变量或响应式状态，支撑页面数据和交互流程。
const router = useRouter();
// 行注释：声明变量或响应式状态，支撑页面数据和交互流程。
const collapsed = ref(false);

// 行注释：声明变量或响应式状态，支撑页面数据和交互流程。
const userRole = computed(() => currentRole());

// 行注释：定义角色中文映射，让顶部身份标签不再显示英文编码。
const roleLabelMap = {
  // 行注释：admin 表示系统管理员，拥有全部管理权限。
  admin: '管理员',
  // 行注释：manager 表示农场管理员，可以维护业务数据。
  manager: '农场管理员',
  // 行注释：viewer 表示只读用户，只能查看数据。
  viewer: '只读用户'
// 行注释：结束对象配置，完成角色编码到中文名称的对应关系。
};

// 行注释：定义旧显示名中文映射，兼容浏览器本地仍缓存英文用户信息的情况。
const displayNameMap = {
  // 行注释：把旧版管理员英文显示名转成中文。
  'System Admin': '系统管理员',
  // 行注释：把旧版农场管理员英文显示名转成中文。
  'Farm Manager': '农场管理员',
  // 行注释：把旧版只读用户英文显示名转成中文。
  'Read Only': '只读用户'
// 行注释：结束对象配置，完成旧英文显示名到中文显示名的对应关系。
};

// 行注释：声明变量 userRoleLabel，用于在页面右上角显示中文角色名称。
const userRoleLabel = computed(() => roleLabelMap[userRole.value] || userRole.value);

// 行注释：声明变量 userDisplayName，用于在页面右上角显示中文用户名称。
const userDisplayName = computed(() => {
  // 行注释：读取当前登录用户对象，可能来自登录接口或浏览器本地缓存。
  const user = authState.user;
  // 行注释：如果没有登录用户，则显示未登录。
  if (!user) return '未登录';
  // 行注释：优先取显示名称，其次取用户名，保证页面始终有可展示内容。
  const rawName = user.display_name || user.username;
  // 行注释：把旧英文显示名映射为中文；如果已经是中文则原样返回。
  return displayNameMap[rawName] || rawName || '未登录';
// 行注释：结束当前脚本代码块。
});

// 行注释：声明变量或响应式状态，支撑页面数据和交互流程。
const menuItems = computed(() => {
  // 行注释：声明变量或响应式状态，支撑页面数据和交互流程。
  const base = [
    // 行注释：设置样式属性，控制页面元素的展示效果。
    { key: '/', icon: () => h(DashboardOutlined), label: '控制台' },
    // 行注释：设置样式属性，控制页面元素的展示效果。
    { key: '/monitoring', icon: () => h(ThunderboltOutlined), label: '能源监测' },
    // 行注释：设置样式属性，控制页面元素的展示效果。
    { key: '/optimization', icon: () => h(BulbOutlined), label: '节能管理' },
    // 行注释：设置样式属性，控制页面元素的展示效果。
    { key: '/carbon', icon: () => h(CloudOutlined), label: '碳排放' },
    // 行注释：设置样式属性，控制页面元素的展示效果。
    { key: '/forecast', icon: () => h(LineChartOutlined), label: '预测计划' },
    // 行注释：设置样式属性，控制页面元素的展示效果。
    { key: '/reports', icon: () => h(FileTextOutlined), label: '报告中心' }
  // 行注释：结束数组配置，完成列表、菜单或图表数据的组装。
  ];

  // 行注释：根据条件控制前端流程，例如权限判断、跳转或错误处理。
  if (userRole.value === 'admin') {
    // 行注释：设置样式属性，控制页面元素的展示效果。
    base.push({ key: '/users', icon: () => h(TeamOutlined), label: '用户管理' });
    // 行注释：设置样式属性，控制页面元素的展示效果。
    base.push({ key: '/audit', icon: () => h(AuditOutlined), label: '审计日志' });
  // 行注释：结束当前脚本代码块。
  }
  // 行注释：返回处理结果，交给调用方或路由守卫继续使用。
  return base;
// 行注释：结束当前脚本代码块。
});

// 答辩讲解:
// - 函数： onMenuClick
// - 作用：处理侧边菜单点击并跳转到目标路由。
// 行注释：声明变量或响应式状态，支撑页面数据和交互流程。
// 方法用法：页面交互或状态变化时调用 onMenuClick，用于完成当前前端模块的一段处理逻辑。
const onMenuClick = ({ key }) => {
  // 行注释：承接上方业务语句，继续完成当前流程的数据组装或调用。
  router.push(key);
// 行注释：结束当前脚本代码块。
};

// 答辩讲解:
// - 函数： logout
// - 作用：调用退出接口，并在任何响应情况下清理本地会话状态。
// 行注释：声明变量或响应式状态，支撑页面数据和交互流程。
// 方法用法：页面交互或状态变化时调用 logout，用于完成当前前端模块的一段处理逻辑。
const logout = async () => {
  // 行注释：承接上方业务语句，继续完成当前流程的数据组装或调用。
  try {
    // 行注释：发起接口请求或读取对象数据，获取页面需要的业务信息。
    await api.post('/auth/logout');
  // 行注释：结束 try 代码块并进入异常处理流程。
  } catch {
    // 忽略退出接口异常，仍然清理本地会话。
  // 行注释：承接上方业务语句，继续完成当前流程的数据组装或调用。
  } finally {
    // 行注释：清空本地登录状态，防止继续使用过期 token。
    clearSession();
    // 行注释：承接上方业务语句，继续完成当前流程的数据组装或调用。
    router.replace('/login');
  // 行注释：结束当前脚本代码块。
  }
// 行注释：结束当前脚本代码块。
};
// 行注释：承接上方业务语句，继续完成当前流程的数据组装或调用。
</script>

<!-- 行注释：进入样式区域，下面控制当前页面的布局和外观。 -->
<style scoped>
/* 行注释：选中页面元素或组件区域，准备编写它的样式。 */
.logo-wrap {
  /* 行注释：设置间距，让页面内容保持清晰层次。 */
  margin: 16px;
  /* 行注释：设置边框或阴影，增强卡片和区域的层次感。 */
  border-radius: 8px;
  /* 行注释：设置背景颜色或渐变，提升页面视觉表现。 */
  background: rgba(255, 255, 255, 0.1);
  /* 行注释：设置当前样式属性，控制页面展示效果。 */
  text-align: center;
  /* 行注释：设置间距，让页面内容保持清晰层次。 */
  padding: 14px 8px;
/* 行注释：结束当前样式规则。 */
}

/* 行注释：选中页面元素或组件区域，准备编写它的样式。 */
.logo-wrap h2 {
  /* 行注释：设置间距，让页面内容保持清晰层次。 */
  margin: 0;
  /* 行注释：设置文字颜色或字体，让页面内容更易阅读。 */
  color: #fff;
  /* 行注释：设置文字颜色或字体，让页面内容更易阅读。 */
  font-size: 18px;
/* 行注释：结束当前样式规则。 */
}

/* 行注释：选中页面元素或组件区域，准备编写它的样式。 */
.header {
  /* 行注释：设置背景颜色或渐变，提升页面视觉表现。 */
  background: #fff;
  /* 行注释：设置间距，让页面内容保持清晰层次。 */
  padding: 0 20px;
  /* 行注释：设置布局方式，控制页面元素如何排列。 */
  display: flex;
  /* 行注释：设置当前样式属性，控制页面展示效果。 */
  align-items: center;
  /* 行注释：设置当前样式属性，控制页面展示效果。 */
  justify-content: space-between;
  /* 行注释：设置边框或阴影，增强卡片和区域的层次感。 */
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
/* 行注释：结束当前样式规则。 */
}

/* 行注释：选中页面元素或组件区域，准备编写它的样式。 */
.header h3 {
  /* 行注释：设置间距，让页面内容保持清晰层次。 */
  margin: 0;
  /* 行注释：设置文字颜色或字体，让页面内容更易阅读。 */
  font-size: 20px;
  /* 行注释：设置文字颜色或字体，让页面内容更易阅读。 */
  font-weight: 600;
/* 行注释：结束当前样式规则。 */
}

/* 行注释：选中页面元素或组件区域，准备编写它的样式。 */
.header-right {
  /* 行注释：设置布局方式，控制页面元素如何排列。 */
  display: flex;
  /* 行注释：设置当前样式属性，控制页面展示效果。 */
  align-items: center;
  /* 行注释：设置间距，让页面内容保持清晰层次。 */
  gap: 8px;
/* 行注释：结束当前样式规则。 */
}

/* 行注释：选中页面元素或组件区域，准备编写它的样式。 */
.username {
  /* 行注释：设置文字颜色或字体，让页面内容更易阅读。 */
  color: #4b5563;
/* 行注释：结束当前样式规则。 */
}

/* 行注释：选中页面元素或组件区域，准备编写它的样式。 */
.content-wrap {
  /* 行注释：设置宽高，让组件在页面中保持稳定尺寸。 */
  min-height: 360px;
  /* 行注释：设置背景颜色或渐变，提升页面视觉表现。 */
  background: #fff;
  /* 行注释：设置边框或阴影，增强卡片和区域的层次感。 */
  border-radius: 8px;
  /* 行注释：设置间距，让页面内容保持清晰层次。 */
  padding: 24px;
/* 行注释：结束当前样式规则。 */
}
/* 行注释：设置当前样式属性，控制页面展示效果。 */
</style>
